version: 2
jobs:
  build:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run:
            name: prep venv and run
            command: |
                virtualenv venv
                . venv/bin/activate
                pip install -r requirements.txt
                pytest test_main.py -v